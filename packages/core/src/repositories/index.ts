/**
 * Repository implementations for rich domain model
 *
 * Repository pattern implementations for PRD, Workspace, and Task entities.
 * All repositories only accept complete entities (no partial updates).
 */

export { PRDRepository } from "./prd-repository";
export { WorkspaceRepository } from "./workspace-repository";
